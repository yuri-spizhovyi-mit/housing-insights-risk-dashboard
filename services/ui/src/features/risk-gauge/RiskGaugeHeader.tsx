function RiskGaugeHeader() {
  return (
    <div className="flex items-center justify-between mb-4 sm:mb-8 flex-wrap gap-2">
      <h2 className="font-semibold">Risk Gauge (macro + local)</h2>
      <span className="text-xs opacity-60">composite index (stub)</span>
    </div>
  );
}

export default RiskGaugeHeader;
