[project]
name = "hird-ml"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pandas",
    "numpy",
    "scikit-learn",
    "prophet",
    "lightgbm",
    "transformers",
    "fastapi",
    "uvicorn",
    "jinja2",
]
